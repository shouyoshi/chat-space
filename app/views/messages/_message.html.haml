.body-message
  .first-message
    %p.name
      = message.user.name
    %p.date
      = message.created_at
  .main-message
    - if message.content.present?
      %p.message 
        = message.content
    = image_tag message.image.url, class: 'Message__image' if message.image.present?